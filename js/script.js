'use strict'

const DomElement = function (selector, height, width, bg, fontSize) {
    let newElement
    this.selector = () => {
        if (selector[0] == '.') {
            newElement = 
        }
    }



    this.height = height
    this.width = width
    this.bg = bg
    this.fontSize = fontSize
}